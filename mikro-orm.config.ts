// Arquivo raiz só pra descoberta automática da CLI do MikroORM
// (`mikro-orm migration:create`, etc.) — a config de verdade vive junto
// do resto da configuração da aplicação, em src/config/.
export { default } from './src/config/mikro-orm.config';
